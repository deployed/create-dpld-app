# create-dpld-app

## development

1. Install dependencies

  ```bash
  yarn
  ```

1. Start a dev build that watches for changes

  ```bash
  yarn dev
  ```

1. You can now run the cli locally

  ```bash
  ./dist/index.js
  ```
